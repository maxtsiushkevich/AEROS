services:
  # flights:
  #   build: .
  #   ports: 
  #     - "3000:3000"

  postgres:
    image: postgres:16
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "flights"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "123"
